# Copy this file to .env.local and adjust values for your local setup.
# .env.local is gitignored — changes here won't affect other developers.

# Set to "false" to hit the real backend instead of mocks.
# Set to "true" to enable MSW (Mock Service Worker) for mocking API requests.
EXPO_PUBLIC_MSW_ENABLED=false
